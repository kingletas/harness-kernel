# Changelog

## 0.1.0

Extracted from `~/Development/typescript/houndbot`, where it was built as the
shared half of a harness that tested a Magento storefront and the Hound backend
from one package. The full history to that point is in that repository.

The kernel knows no target: the registry and the workspace are handed to it.
